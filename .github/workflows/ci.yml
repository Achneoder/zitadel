name: CI 

on:
  push:
    tags-ignore:
      - "*"
    branches:
      - "main"
      - "next"
      - "next-rc"
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  packages: write
  issues: write
  pull-requests: write

jobs:

  release:
    uses: ./.github/workflows/release.yml
    permissions:
      packages: write
      contents: write
      issues: write
      pull-requests: write
    if: ${{ github.event_name == 'workflow_dispatch' || github.ref_name == 'next' || github.ref_name == 'next-rc' }}
    with:
      node_version: "22"
      semantic_version: "23.0.7"
      image_name_github_login: "ghcr.io/zitadel/zitadel-login"
